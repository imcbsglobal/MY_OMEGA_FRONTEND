# Attendance Calendar - Architecture & Data Flow

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                       PAYROLL PAGE                           │
│  (my_omega_newfrontend/src/components/Payroll/PayrollPage)  │
└─────────────────────────────────────────────────────────────┘
                             │
                             │ imports
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  ATTENDANCE CALENDAR                         │
│ (my_omega_newfrontend/src/components/Payroll/             │
│  AttendanceCalendar.jsx)                                    │
│                                                             │
│  • Calendar Grid with Color Coding                          │
│  • Statistics Cards (Working, Paid, Unpaid, Absent)         │
│  • Legend & Summary Table                                   │
└─────────────────────────────────────────────────────────────┘
                             │
                             │ API Call
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      BACKEND API                            │
│        GET /api/hr/attendance/                              │
│                                                             │
│  Query Parameters:                                          │
│  • user_id={employeeId}                                     │
│  • month={monthNumber}                                      │
│  • year={yearNumber}                                        │
└─────────────────────────────────────────────────────────────┘
                             │
                             │ Response
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                   DATABASE (Django)                         │
│        HR.models.Attendance                                 │
│                                                             │
│  Stores:                                                    │
│  • user_id, date, status                                    │
│  • is_paid_leave, leave_type                                │
│  • punch_in/out times                                       │
│  • verification_status                                      │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│ 1. USER SELECTS EMPLOYEE                                    │
│    PayrollPage: handleSelectEmployee()                      │
│    Sets: selectedEmployee, hasEmployeeSelected=true         │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. MONTH/YEAR SELECTION                                     │
│    PayrollPage: useEffect triggers                          │
│    Sets: selectedMonth, selectedYear                        │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. ATTENDANCE CALENDAR RECEIVES PROPS                        │
│    Props:                                                   │
│    • employeeId={selectedEmployee.id}                       │
│    • month={selectedMonth}                                  │
│    • year={selectedYear}                                    │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. COMPONENT USEEFFECT TRIGGERS                             │
│    Calls: fetchAttendanceCalendar()                         │
│    State: loading=true                                      │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. API REQUEST SENT                                         │
│    GET /api/hr/attendance/                                  │
│    ?user_id=123&month=1&year=2026                           │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. BACKEND PROCESSES REQUEST                                │
│    AttendanceViewSet.get_queryset()                         │
│    Filters by: user_id, month, year                         │
│    Returns: [Attendance objects]                            │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. JSON RESPONSE RECEIVED                                   │
│    [                                                        │
│      { id: 1, date: "2026-01-01", status: "full", ... }    │
│      { id: 2, date: "2026-01-02", status: "casual_leave" } │
│      ...                                                    │
│    ]                                                        │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. DATA PROCESSING                                          │
│    processAttendanceData():                                 │
│    • Map status to colors                                   │
│    • Calculate statistics                                   │
│    • Create calendar grid data                              │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 9. STATE UPDATES                                            │
│    setCalendarData(calendar)                                │
│    setStats(statsObj)                                       │
│    setLoading(false)                                        │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│ 10. COMPONENT RE-RENDERS                                    │
│     Displays:                                               │
│     • Statistics Cards                                      │
│     • Calendar Grid                                         │
│     • Legend                                                │
│     • Summary Table                                         │
└─────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
PayrollPage
├── Employee Selection Dropdown
├── Month & Year Selectors
├── Salary Breakdown Section
│   ├── Basic Salary Card
│   ├── Allowances Card
│   ├── Deductions Card
│   └── Net Salary Card
│
├── 🆕 ATTENDANCE CALENDAR (NEW)
│   ├── Header with Title
│   ├── Statistics Cards Section
│   │   ├── Working Days Card
│   │   ├── Paid Leave Card
│   │   ├── Unpaid Leave Card
│   │   └── Absent Days Card
│   ├── Calendar Grid
│   │   ├── Week Headers (Sun-Sat)
│   │   └── Day Cells (1-31)
│   ├── Legend Section
│   │   └── Status Color Reference
│   └── Summary Table
│       ├── Working Days Row
│       ├── Paid Leave Row
│       ├── Unpaid Leave Row
│       ├── Holidays Row
│       └── Absent Days Row
│
├── Allowances Details Section
├── Deductions Details Section
└── Proceed Button
```

## State Management

```
AttendanceCalendar Component States:

┌─────────────────────────────┐
│  calendarData (Object)      │
│  {                          │
│    1: { status, label, color} │
│    2: { status, label, color} │
│    ... (up to 31 days)      │
│  }                          │
└─────────────────────────────┘

┌─────────────────────────────┐
│  stats (Object)             │
│  {                          │
│    presentDays              │
│    absentDays               │
│    leaveDays                │
│    paidLeaveDays            │
│    unpaidLeaveDays          │
│    workingDays              │
│    weekends                 │
│    holidays                 │
│  }                          │
└─────────────────────────────┘

┌─────────────────────────────┐
│  loading (Boolean)          │
│  error (String)             │
└─────────────────────────────┘
```

## Color Processing Pipeline

```
Backend Status Value
        │
        ▼
┌──────────────────────────┐
│ Status Mapping           │
│ (statusColors Object)    │
└──────────────────────────┘
        │
        ├─ "full" ──────────→ #10B981 (Green)
        ├─ "half" ──────────→ #3B82F6 (Blue)
        ├─ "absent" ────────→ #EF4444 (Red)
        ├─ "casual_leave" ──→ #F59E0B (Orange)
        ├─ "sick_leave" ────→ #8B5CF6 (Purple)
        ├─ "special_leave" →→ #EC4899 (Pink)
        ├─ "unpaid_leave" ──→ #6B7280 (Gray)
        ├─ "mandatory_holiday" → #059669 (Dark Green)
        ├─ "special_holiday" ──→ #0891B2 (Cyan)
        ├─ "sunday" ────────→ #D946EF (Magenta)
        ├─ "wfh" ──────────→ #06B6D4 (Light Cyan)
        └─ "weekend" ───────→ #E5E7EB (Light Gray)
        │
        ▼
┌──────────────────────────┐
│ Calendar Day Object      │
│ {                        │
│   status: "full",        │
│   label: "Present",      │
│   color: "#10B981"       │
│ }                        │
└──────────────────────────┘
        │
        ▼
┌──────────────────────────┐
│ Render Calendar Cell     │
│ with backgroundColor:    │
│ color and border         │
│ {color}                  │
└──────────────────────────┘
```

## Statistics Calculation Flow

```
Attendance Records Array
        │
        ▼
┌──────────────────────────┐
│ Initialize Stats         │
│ All counters = 0         │
└──────────────────────────┘
        │
        ▼
┌──────────────────────────┐
│ For Each Record:         │
│ Read status field        │
└──────────────────────────┘
        │
        ▼
    Status Switch
        │
    ┌───┴────────────────────┐
    │                        │
    ▼                        ▼
   "full"                 "half"
   ↓                      ↓
  +1 presentDays          +1 presentDays
  +1 workingDays          +0.5 workingDays
    │                        │
    └────────┬───────────────┘
             │
             ▼
    ┌──────────────────┐
    │ Continue with    │
    │ next record      │
    └──────────────────┘
    │
    └─→ Repeat until
        all records
        processed
             │
             ▼
    ┌──────────────────────┐
    │ Final Stats Object   │
    │ with all totals      │
    └──────────────────────┘
             │
             ▼
    Render Statistics Cards
```

## API Integration Details

```
┌─────────────────────────────────────────┐
│ HTTP Request                            │
├─────────────────────────────────────────┤
│ Method: GET                             │
│ URL: /api/hr/attendance/                │
│ Query Params:                           │
│   user_id=123 (employee ID)             │
│   month=1     (1-12)                    │
│   year=2026   (YYYY)                    │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Django Backend Processing               │
├─────────────────────────────────────────┤
│ AttendanceViewSet:                      │
│ 1. Check permissions                    │
│ 2. Parse query parameters               │
│ 3. Filter queryset by:                  │
│    - user_id (if admin)                 │
│    - date__month=month                  │
│    - date__year=year                    │
│ 4. Serialize objects                    │
│ 5. Return JSON array                    │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ JSON Response                           │
├─────────────────────────────────────────┤
│ [                                       │
│   {                                     │
│     "id": 1,                            │
│     "user": 123,                        │
│     "date": "2026-01-01",               │
│     "status": "full",                   │
│     "is_paid_leave": false,             │
│     "leave_type": null,                 │
│     ...                                 │
│   },                                    │
│   ...                                   │
│ ]                                       │
└─────────────────────────────────────────┘
```

## Error Handling Flow

```
API Call Attempted
        │
        ├─ Success (Response 200)
        │  └─→ Process Data
        │     └─→ Render Calendar
        │
        └─ Failure (4xx, 5xx, Network)
           │
           ├─ Log error to console
           │
           ├─ Set error state (but don't show UI)
           │
           ├─ Reset calendar data to empty
           │
           └─→ Render Empty Calendar Gracefully
              (User sees empty calendar, not error)
```

## Integration Points

1. **PayrollPage.jsx**
   - Imports AttendanceCalendar component
   - Passes props: employeeId, month, year
   - Triggers on employee selection and date changes

2. **Backend API**
   - Accepts: user_id, month, year query params
   - Returns: Array of Attendance objects
   - Endpoint: /api/hr/attendance/

3. **API Client**
   - Uses: configured axios instance
   - Path: `../../api/client`
   - Auto-handles auth and base URL

4. **Styling**
   - Inline styles (React style objects)
   - Matches PayrollPage design language
   - Responsive grid layouts
